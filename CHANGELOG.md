# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.1] — 2026-09-15

体验修补 + 两个用户报告的显示问题。没有新概念、没有新依赖。

### 新增

- **收藏夹**：侧栏的 ⭐ 区可以新建任意多个收藏夹，支持重命名与删除，
  数量不限。和内置分类完全同权 —— 卡片能拖进去，桌面条目拖进去也不会被拽回
  「桌面」。**只存映射**：新建不建目录，删除不动文件，删掉后里面的桌面条目
  会被同步放回「桌面」分类
- **全局快捷键 `Ctrl + Alt + Q`**：面板关着时唤出，已展开则收起，形成 Toggle。
  用带 Ctrl 的组合是因为纯 `Alt` 组合容易被别的常驻程序先占走 ——
  本机实测 `Alt+Q` / `Alt+Z` 都被 ASUS 的后台服务注册着，`RegisterHotKey` 直接失败。
  注册在自己线程的消息循环上（`RegisterHotKey` 的 `WM_HOTKEY` 会投递到注册
  线程，主线程的消息循环归 tao 所有，投过去收不到）。该键被占用时只记日志，
  不影响其余功能
- **面板进出动画**：从屏幕底边滑出 / 滑回，配合淡入淡出，约 180–200 ms

### 修复

- **收起时整个窗口闪现一下**（用户报告的"黑闪"）。根因是 `SetWindowRgn(hwnd, NULL)`
  在窗口可见时把裁剪区域整个丢掉，模块之间那几条从未被绘制过的缝重新暴露出来
- **偶发浮现 Windows 原生标题栏**。根因有两个，都修在结构层面：
  1. tao 创建无边框窗口时**没有**去掉 `WS_CAPTION`，它靠 `WM_NCCALCSIZE` 返回 0
     把非客户区压成零高度。现在每次显示/隐藏后都把 `WS_CAPTION | WS_SYSMENU |
     WS_MINIMIZEBOX | WS_MAXIMIZEBOX` 从窗口样式里真的摘掉，非客户区恒为零，
     `DefWindowProc` 想画也没有地方画
  2. tao 每次 show/hide 都会用 `to_window_styles()` 重写窗口样式，而它带的
     `SetWindowPos(SWP_FRAMECHANGED)` **会把自定义窗口区域一起丢掉**。实测
     show 之后有约 14 ms 窗口是完全没有裁剪的 —— 现在显示后立刻用记住的
     「基准矩形 + 当前位移」把区域补回来
- **收起末尾残留一片"玻璃"**（用户报告的"黑色残留"）。淡出原先只改 CSS 的
  `opacity`，那管得到 webview 画的内容，管不到 `DwmEnableBlurBehindWindow`
  施加的那层原生玻璃 —— 面板内容淡到全透明之后玻璃还留在屏幕上，直到窗口隐藏
  才突然消失。改用 `WS_EX_LAYERED` + `SetLayeredWindowAttributes(LWA_ALPHA)`
  逐帧驱动**整窗**不透明度，玻璃和内容一起淡。白底实测：修前收尾约 50 ms 里
  模块区域比桌面本底暗 55 个色阶，修后与桌面本底逐点相等
- 动画首帧可能越过起点（rAF 回调的时间戳早于记录的起点，进度变成负数）
- 启动时窗口创建到 `setup` 之间会以带标题栏的样式可见；现在窗口以
  `visible: false` 创建，第一次显示就已经是无边框的
- tao 在 `show()` 之后约 10 ms 会用 `to_window_styles()` 重写窗口样式，把
  `WS_EX_LAYERED` 一起冲掉 —— 那之后每一次 `SetLayeredWindowAttributes` 都会
  失败，淡出会**整个失效**（面板一路不透明，最后硬切）。现在样式被抢走时会
  自己补回来再重试，不依赖 show 之后的补救顺序

### 变更

- 出动画只改 `transform`，不碰 `width` / `height` / `top` / `left`；
  原生裁剪区域跟着 CSS 位移同帧更新，滑动过程中面板不会被旧区域切掉一条边
- 淡出从 CSS `opacity` 挪到窗口自身的 alpha（见上），两者不再各走一套曲线；
  DOM 的 `opacity` 退化成一道只在"完全收起"那一档落下的闸门，用来盖住
  `show()` 之后那段扩展样式还没补回来、整窗不透明的空窗
- 窗口尺寸与面板一致（1400×860），消除启动时的一次 resize

## [0.1.0] — 2026-09-14

首个可发布版本。

### 新增

**桌面入口管理**

- 自动扫描 Windows 桌面（用户桌面 + 公共桌面），用 `SHGetKnownFolderPath` 解析路径，
  能正确处理 OneDrive 重定向
- 全量同步：桌面新增的自动导入，桌面删除的自动移除
- 「桌面」分类由同步引擎掌管，条目可拖到人工分类长期保留
- 忽略名单：在 Portal 里移除的桌面条目不再自动导入
- 最近访问记录（上限 20 条）

**交互**

- 边缘唤醒：鼠标贴住屏幕底边即展开，离开后自动收起
- 收起时窗口被**整个隐藏**，屏幕上零像素残留
- 拖拽归类：卡片拖到左侧导航即可改分类，只改映射、不碰文件
- 从资源管理器拖文件/文件夹进窗口即添加

**图标**

- Windows 原生图标提取：`.lnk` 解析到目标图标、`.exe` 提取内嵌资源、
  文件夹用系统图标、其他按扩展名关联
- 无 Emoji 代替方案；取不到图标时退回几何图形

**系统集成**

- 隐藏 / 恢复桌面图标（系统级开关），**退出时自动恢复**
- 崩溃 / 强杀后下次启动自动对账恢复
- 开机启动开关

**界面**

- Windows 11 Fluent 浅色主题：白色磨砂面板、系统强调色
- 三个独立悬浮模块：分类导航 / 内容区 / 最近访问
- 弹簧动画、柔和投影、圆角

### 数据安全

- 配置原子写：临时文件 + `fsync` + 一代备份 + `rename`
- 配置损坏时进入**只读模式**并隔离原文件，绝不静默重置
- schema 版本迁移前留快照

### 已知限制

- 模块间的缝隙显示的是被模糊的桌面，不是原始桌面
- 面板下边缘贴住屏幕底边，无法真正居中悬浮
- 四个固定分类不支持增删（收藏夹可以，见「未发布」）
- 图标使用 32×32 系统大图标，高 DPI 下略有放大模糊
- 不含搜索

### 不变量

整个项目建立在四条不变量上，任何版本都不会违反：

1. 不移动任何文件
2. 不修改任何文件路径
3. 不复制文件
4. 只保存映射关系，桌面文件永远是真实来源

卸载或删除配置目录即可完全恢复原状，磁盘上的文件从未被触碰。
